# ==================================
# CONTROLNOT V2 - PYTHON DEPENDENCIES
# ==================================

# Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0  # Servidor WSGI con soporte para Uvicorn workers
python-multipart==0.0.6
pydantic>=2.10.0
pydantic-settings>=2.6.0

# AI Providers
openai>=1.30.0  # Semana 1: Structured Outputs (0 errores JSON)
anthropic>=0.40.0  # Actualizado para Python 3.13+
httpx>=0.27.0  # Actualizado - sin versión fija

# Google Cloud (OCR + Drive) - Actualizados para Python 3.13
google-cloud-vision>=3.7.0
google-api-python-client>=2.140.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
google-api-core>=2.19.0

# Document Generation
python-docx==1.1.0

# Image Processing (Claude Vision + OCR Robusto 2025)
Pillow>=10.0.0  # Preprocesamiento de imágenes para Claude Vision
opencv-python-headless>=4.8.0  # CLAHE, denoise, binarización adaptativa
numpy>=1.24.0  # Requerido por OpenCV

# Text Matching (Validación anti-alucinación)
rapidfuzz>=3.5.0  # Fuzzy matching moderno (reemplazo de fuzzywuzzy)

# Utils
python-dotenv==1.0.0
structlog==24.1.0
aiofiles==23.2.1
redis==5.0.1  # Semana 1: Redis Caching (-50% duplicados)

# Email
aiosmtplib==3.0.1
email-validator==2.1.0

# Date handling
python-dateutil==2.8.2

# Database
supabase>=2.10.0  # Actualizado para compatibilidad httpx

# Testing (dev)
pytest==7.4.3
pytest-asyncio==0.21.1
